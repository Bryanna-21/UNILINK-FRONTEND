import { useState } from "react";
import { Link,useNavigate } from "react-router-dom";
import { useAuth } from "../../context/AuthContext";
import { FaArrowRight,FaLock,FaEnvelope } from "react-icons/fa";
import "../../styles/Auth.css";
export default function Login(){ const {login}=useAuth(); const navigate=useNavigate(); const [email,setEmail]=useState(""); const [password,setPassword]=useState(""); const [error,setError]=useState(""); const [loading,setLoading]=useState(false); const submit=async e=>{e.preventDefault();setError("");if(!email.trim()||!password){setError("Enter your email and password.");return;}setLoading(true);const r=await login(email.trim(),password);setLoading(false);if(r.success)navigate('/student/dashboard',{replace:true});else if(r.reason==='requiresTwoFactor')navigate('/verify-login-otp',{state:{userId:r.userId,email:email.trim()},replace:true});else if(r.reason==='requiresVerification')navigate('/verify-otp',{state:{userId:r.userId,email:email.trim()},replace:true});else setError(r.message);}; return <div className="auth-container"><div className="auth-box auth-modern"><div className="auth-brand">UniLink</div><p className="auth-kicker">STUDENT PORTAL</p><h1>Welcome back</h1><p className="subtitle">Your courses, community and study tools in one place.</p>{error&&<div className="error-message">{error}</div>}<form onSubmit={submit} className="auth-form"><label>Email<div className="input-wrap"><FaEnvelope/><input value={email} onChange={e=>setEmail(e.target.value)} type="email" autoComplete="email" placeholder="you@university.ac.ke" disabled={loading}/></div></label><label>Password<div className="input-wrap"><FaLock/><input value={password} onChange={e=>setPassword(e.target.value)} type="password" autoComplete="current-password" placeholder="Your password" disabled={loading}/></div></label><button className="auth-button" disabled={loading}>{loading?'Signing in…':<>Sign in <FaArrowRight/></>}</button></form><div className="auth-links"><Link to="/register">Create student account</Link><span>·</span><Link to="/register">Need help?</Link></div><p className="security-note">New device sign-ins may require a one-time verification code.</p></div></div>; }
