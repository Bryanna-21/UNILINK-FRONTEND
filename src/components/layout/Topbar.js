import { FaBars, FaBell, FaSearch, FaSun, FaMoon } from "react-icons/fa";
import { useAuth } from "../../context/AuthContext";
import { useTheme } from "../../context/ThemeContext";
import { useNavigate } from "react-router-dom";
import "../../styles/layout/topbar.css";
export default function Topbar({onMenu}){ const {user}=useAuth(); const {theme,toggleTheme}=useTheme(); const navigate=useNavigate(); return <header className="topbar"><button className="mobile-menu-btn" onClick={onMenu} aria-label="Open navigation"><FaBars/></button><button className="brand-mobile" onClick={()=>navigate('/student/dashboard')} aria-label="UniLink home">UniLink</button><div className="topbar-search"><FaSearch/><input type="search" placeholder="Search courses, units, posts..." aria-label="Search" onKeyDown={e=>{if(e.key==='Enter'&&e.currentTarget.value.trim()) navigate(`/search?q=${encodeURIComponent(e.currentTarget.value.trim())}`)}}/></div><div className="topbar-right"><button className="icon-btn" onClick={toggleTheme} aria-label={`Switch to ${theme==='dark'?'light':'dark'} mode`}>{theme==='dark'?<FaSun/>:<FaMoon/>}</button><button className="notification-btn" onClick={()=>navigate('/notifications')} aria-label="Notifications"><FaBell/></button><button className="profile-box" onClick={()=>navigate('/profile')} aria-label="Open profile"><div className="topbar-avatar">{(user?.name||"U").slice(0,1).toUpperCase()}</div><div className="profile-copy"><strong>{user?.name||"Student"}</strong><span>Student</span></div></button></div></header>; }
